/**
 * CONFIGURACIÓN DE FIREBASE — Tecno Caja Contadores
 * ==================================================
 * Pasos para completar:
 * 1. Ve a https://console.firebase.google.com
 * 2. Selecciona tu proyecto → ⚙️ Configuración → Tus apps → Web app
 * 3. Si no tienes una web app creada, crea una (nombre: "Tecno Caja Contadores")
 * 4. Copia los valores del objeto firebaseConfig y pégalos aquí
 *
 * Nota: este archivo se carga ANTES de firebase-app-compat.js
 * La app usará /api/firebase-config para obtener las variables del .env
 * si FIREBASE_CONFIG no está definido aquí (modo preferido).
 */
window.FIREBASE_CONFIG = null; // Se carga dinámicamente desde /api/firebase-config
