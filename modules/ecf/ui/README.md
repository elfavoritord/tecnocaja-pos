La interfaz administrativa reaprovecha el modal existente del POS, pero ahora consume el backend limpio bajo `/api/ecf`.

Aquí pueden vivir más adelante componentes dedicados o assets específicos del panel e-CF.
