<?xml version="1.0" encoding="utf-8"?>
<!--
  Por defecto NO se permite trafico sin cifrar (HTTPS obligatorio) para la nube de
  Tecno Caja. Se habilita cleartext SOLO para rangos de IP privados/locales, porque
  el modo "vinculacion en red local" conecta la app directamente al servicio Express
  del POS Windows (server.js, puerto 3399) dentro de la misma red LAN del negocio,
  igual que POS_ALLOW_LAN en el backend. El endpoint de sincronizacion en la nube
  (api.tecnocaja.app o el dominio que se configure) siempre debe ser HTTPS: eso se
  fuerza en codigo (ver core/network/api_client.dart), este archivo solo abre la
  puerta a nivel de SO para que Android no bloquee la conexion LAN.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <!--
      Nota: dart:io (usado por Dio) no respeta este archivo para conexiones directas
      por socket: el bloqueo real de "HTTPS obligatorio salvo LAN privada" vive en
      ApiClient.validateBaseUrl (core/network/api_client.dart), que se ejecuta antes
      de cada request. Este XML cubre las rutas que si pasan por el stack nativo de
      Android (WebView de OAuth, deep links, etc).
    -->
</network-security-config>
