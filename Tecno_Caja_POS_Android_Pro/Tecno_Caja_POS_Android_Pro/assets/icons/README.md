# Iconos

Carpeta para iconos adicionales (categorias, metodos de pago, etc). El icono de la app (launcher) se genera desde `android/app/src/main/res/mipmap-*` con `flutter_launcher_icons`; ver `docs/BUILD_RELEASE.md`.
